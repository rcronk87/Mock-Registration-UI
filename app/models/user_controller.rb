class UserController
  def initialize(pass, user, email, country, zipcode, fullname, address, city, state)
    @pass = pass
    @user = user
    @email = email
    @country = country
    @zipcode = zipcode
    @fullname = fullname
    @address = address
    @city = city
    @state = state
  end
  

end
